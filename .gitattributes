# 文本文件统一 LF，避免 Godot 场景 / 脚本在跨平台协作时产生无意义的整文件 diff
* text=auto eol=lf

# Windows 批处理必须 CRLF —— 裸 LF 会让 cmd.exe 解析错位，
# 把 rem 注释里的单词当成命令执行。别改这两行。
*.cmd text eol=crlf
*.bat text eol=crlf

# Windows 批处理还必须保持纯 ASCII：含非 ASCII 时 cmd 按 OEM 代码页解码会乱码。

# 二进制资源不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ogg binary
*.wav binary
*.mp3 binary
*.ttf binary
*.otf binary
*.exe binary
*.pck binary
*.zip binary
